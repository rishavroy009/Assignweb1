sudo apt-get install build-essential git cmake libxml2-dev zlib1g-dev libssl-dev


git clone https://github.com/gpac/gpac.git
cd gpac

./configure
make -j$(nproc)  # This uses all available CPU cores for faster compilation


sudo make install
