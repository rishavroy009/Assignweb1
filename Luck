#include<bits/stdc++.h>
using namespace std;
int main(){
    long long n;int k;cin>>n>>k;
    vector<long long>a(k);
    for(int i=0;i<k;i++)cin>>a[i];
    sort(a.begin(),a.end());
    queue<long long>q;
    unordered_map<long long,long long>p;
    q.push(1);p[1]=0;
    while(!q.empty()&&!p.count(n)){
        long long cur=q.front();q.pop();
        for(auto x:a){
            if(cur>n/x)continue;
            long long nxt=cur*x;
            if(n%nxt)continue;
            if(!p.count(nxt)){p[nxt]=cur;q.push(nxt);}
        }
    }
    if(!p.count(n)){cout<<-1;return 0;}
    vector<long long>path;
    for(long long v=n;v;v=p[v])path.push_back(v);
    reverse(path.begin(),path.end());
    for(int i=0;i<path.size();i++){
        if(i)cout<<" ";
        cout<<path[i];
    }
}
